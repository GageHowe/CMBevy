[package]
name = "client"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
common = { path = "../common" }
bevy = { version = "0.18", default-features = false, features = ["jpeg"] }
bevy_egui = "0.39.1"
zstd = "0.13.3"
