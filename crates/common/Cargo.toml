
[package]
name = "common"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
bevy = { version = "0.18", features = ["serialize", "std", "multi_threaded"] }
serde = { version = "1", features = ["derive"] }
