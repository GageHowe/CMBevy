
[package]
name = "common"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true, features = ["bevy_state", "gamepad", "keyboard", "mouse", "serialize", "std", "multi_threaded"] }
serde = { workspace = true }
