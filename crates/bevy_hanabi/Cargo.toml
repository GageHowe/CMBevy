[package]
name = "bevy_hanabi_plugin"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true, features = ["std", "multi_threaded"] }
hanabi = { package = "bevy_hanabi", version = "0.18.0", default-features = false, features = ["3d", "serde"] }
