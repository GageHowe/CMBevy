# encrypted asset packing

`assets/package.toml` lists copied runtime folders and files replaced by `encrypted/assets.pak`.

For example, `textures/crosshairs/**` still loads as `textures/crosshairs/a.png`.
